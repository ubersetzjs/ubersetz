# ubersetz

Runtime package for **ubersetz**.

This package is responsible for:

- the `u()` translation API
- locale loading and switching
- MessageFormat-based rendering at runtime
- legacy plural compatibility at runtime

For full documentation, API usage, and examples, see the root README:

- [`../../README.md`](../../README.md)
